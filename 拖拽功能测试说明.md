# 小智人拖拽功能测试说明

## 功能优化说明

我已经优化了小智人AI助手的拖拽功能，现在更容易使用了：

### 主要改进

1. **扩大拖拽目标区域**
   - 之前：只能拖拽到输入框区域（很小）
   - 现在：可以拖拽到整个聊天面板的任意位置

2. **添加视觉反馈**
   - 拖拽到面板上时会显示紫色渐变背景提示
   - 显示"📥 松开鼠标即可让小智人分析"的提示文字
   - 图标会有上下弹跳的动画效果

3. **改进拖拽检测**
   - 修复了拖拽到子元素时误触发离开事件的问题
   - 更精确的边界检测

## 测试步骤

### 测试1: 拖拽课件数据

1. 打开 **数据中心** → **资源数据** → **课件教案**
2. 点击右侧的小智人悬浮球，打开AI助手面板
3. 在课件列表中，**点住并拖拽**任意一个课件卡片或列表项
4. 拖拽到右侧的小智人面板任意位置
5. 观察面板变化：
   - 面板应该显示紫色渐变背景
   - 中间显示"📥 松开鼠标即可让小智人分析"
6. 松开鼠标
7. 小智人应该：
   - 自动在输入框填入分析请求
   - 自动发送消息
   - 开始生成课件的详细分析报告

**预期回复内容包括：**
- 📊 课件深度分析报告
- 📚 基本信息
- 🎯 内容质量评估
- 💡 优化建议
- 📈 使用建议

### 测试2: 拖拽班课数据

1. 打开 **数据中心** → **教学数据** → **班课管理**
2. 确保小智人AI助手面板已打开
3. 在课程列表中，**点住并拖拽**任意一行课程数据
4. 拖拽到右侧的小智人面板任意位置
5. 观察面板变化（同上）
6. 松开鼠标
7. 小智人应该：
   - 自动分析该班课的情况
   - 生成综合分析报告

**预期回复内容包括：**
- 📊 班课综合分析报告
- 📚 基本信息（课程编码、类型、学时、学分、进度）
- 📈 教学进度分析
- 🎯 教学建议
- 💡 下一步行动建议

## 调试技巧

如果拖拽不工作，请按照以下步骤排查：

### 1. 检查控制台日志

打开浏览器开发者工具（F12），在 Console 中应该看到：

- 拖拽开始时：`开始拖拽课件: xxx` 或 `开始拖拽班课: xxx`
- 拖拽到面板时：`接收到拖拽数据: {type: 'courseware', ...}`
- 拖拽结束时：`拖拽结束`
- 自动发送时：`✅ 课件已添加到对话框，准备自动发送` 或 `✅ 班课数据已添加到对话框，准备自动发送`

### 2. 检查拖拽数据格式

在 Console 中检查 `接收到拖拽数据` 的日志，应该包含：

**课件数据:**
```json
{
  "type": "courseware",
  "id": "...",
  "name": "课件名称",
  "description": "课件描述",
  "courseName": "所属课程",
  "coursewareType": "课件"
}
```

**班课数据:**
```json
{
  "type": "class",
  "id": "...",
  "code": "课程编码",
  "name": "课程名称",
  "className": "班级名称",
  "courseType": "课程类型",
  "totalHours": 48,
  "totalCredits": 3,
  "progress": 65
}
```

### 3. 常见问题

**问题1: 拖拽时没有视觉反馈**
- 检查小智人面板是否已打开
- 检查是否拖拽到了面板区域内

**问题2: 松开鼠标后没有反应**
- 查看控制台是否有错误
- 确认拖拽数据格式是否正确

**问题3: 拖拽样式闪烁**
- 这是正常的，因为拖拽提示层会覆盖在内容上
- 新的代码已经优化了这个问题

## 技术细节

### 拖拽数据传输格式

使用 HTML5 的 DataTransfer API：
- 数据类型：`application/json`
- 传输效果：`copy`

### 拖拽事件流程

1. **拖拽源（课件/班课列表）**
   - `dragstart` 事件：设置拖拽数据
   - `dragend` 事件：恢复元素样式

2. **拖拽目标（小智人面板）**
   - `dragover` 事件：显示拖拽提示，设置 dropEffect
   - `dragleave` 事件：隐藏拖拽提示（带边界检测）
   - `drop` 事件：接收数据，生成分析请求，自动发送

### 自动分析功能

拖拽完成后会自动：
1. 填充输入框（用户可见提示文本）
2. 调用 `sendMessage()` 自动发送
3. AI 根据数据类型生成对应的分析报告

## 文件修改清单

- `src/components/html-page/AIAssistant.vue`
  - 将拖拽事件从 input-container 移到 panel-content
  - 添加拖拽提示层（drag-hint-overlay）
  - 优化 handleDragLeave 边界检测
  - 更新输入框占位符文本

## 已知限制

1. 拖拽功能仅在打开小智人面板时可用
2. 需要将课件/班课拖拽到面板区域（不能拖拽到其他地方）
3. 每次只能拖拽一个项目

## 后续优化建议

1. 支持拖拽到悬浮球自动打开面板
2. 支持批量拖拽多个项目
3. 添加拖拽历史记录
4. 支持更多类型的数据拖拽（如学生、作业等）

